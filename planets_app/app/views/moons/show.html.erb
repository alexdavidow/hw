<body>
	<div class="translucent" id="moonform">
		<h1>Name: <%= @moon.name %></h1><br>
		Moon Diameter (in km): <%= @moon.diameter %><br>
		Distance from <%= @moon.solar_system.name %> (in km): <%= @moon.distance %><br>
		<% if @moon.solar_system.present? %>
		<p>Orbits the Planet: <%= link_to @moon.solar_system.name, solar_system_path(@moon.solar_system) %></p>
		<% else %>
		This moon isn't orbitting anything, which isn't possible, but go with it.
		<% end %>
		<br>
		<%= link_to 'Edit', edit_moon_path(@moon), method: 'get' %>
		<%= link_to 'Delete', moon_path(@moon), method: 'delete' %>
	</div>
</body>